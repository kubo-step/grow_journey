<%= turbo_stream.remove "tab1_#{dom_id(@task)}" %>
<%= turbo_stream.remove "tab2_#{dom_id(@task)}" %>
<%= turbo_stream.replace "progress_#{@goal.id}", partial: "goals/progress", locals: { goal: @goal } %>
<%= turbo_stream_flash %>