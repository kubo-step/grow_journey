<div class="hero min-h-screen">
  <div class="row">
    <div class="col-12">
      <div class="text-center text-2xl">あなたの現在の本数は〇〇本です</div>
        <div class="card flex-shrink-0 w-full max-w-sm shadow-2xl mx-auto mt-12 mb-12">
          <div class="card-body">
            <p>目標</p><br>
            <% if @goals.present? %>
              <%= render @goals %>
            <% else %>
              <p><%= t('.no_result') %></p>
            <% end %>
            <%= link_to '登録する', new_goal_path, class:'btn btn-secondary'%>
          </div>
        </div>
        <div class="card flex-shrink-0 w-full max-w-sm shadow-2xl mx-auto mt-12 mb-12">
          <div class="card-body">
            タスク
            <% if @goals.present? %>
              <%= render @goals %>
            <% else %>
              <p><%= t('.no_result') %></p>
            <% end %>
            <%= link_to '登録する', new_goal_path, class:'btn btn-secondary'%>
          </div>
        </div>
        </div>
      </div>
    </div>
  </div>
</div>